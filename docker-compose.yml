services:
  node:
    image: "node:18-alpine"
    user: "node"
    working_dir: /src
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/src
    ports:
      - "8081:35295"
    command: "npm start"

networks:
  default:
    name: appnet